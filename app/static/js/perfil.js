// JS para funcionalidades do perfil
// Exemplo: abrir modal de edição, carregar posts, etc.

// Exemplo de ação para botão editar perfil
const btnEditar = document.querySelector('.perfil-info button');
if(btnEditar) {
  btnEditar.addEventListener('click', () => {
    alert('Funcionalidade de edição de perfil em breve!');
  });
}
